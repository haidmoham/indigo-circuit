# Copy this to transform/profiles.yml — values come from env vars set on Railway.
ptcg_scouting:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DUCKDB_PATH', 'data/ptcg.duckdb') }}"
      schema: dbt_dev
      threads: 4
